# 数学建模 Agent 系统 · 技术栈总结

> **一句话定位**：以 LangGraph 状态机为编排内核、FastAPI 为展示适配层、Electron + React 为交互壳的
> 竞赛级数学建模自动化系统——从题目解析、多方法并行求解、质检打回，到 LaTeX 论文成稿与 AI 使用合规留痕的全流程闭环。

---

## 1. 总体架构

系统采用**内核与展示严格分层**的结构：`src/agent.py` 的图逻辑与展示层零耦合，后端只做协议适配（SSE 化），前端只做渲染与交互。

```
┌─────────────────────────────────────────────────────────────┐
│  Electron 壳 (concurrently 驱动 vite + electron)             │
│  React 18 + TS + Vite ── 液态玻璃 UI / 多题会话桶 / SSE 消费  │
└───────────────▲─────────────────────────┬───────────────────┘
                │ SSE (token/reasoning/    │ POST /api/stream
                │ update/interrupt/…)      │ (resume / continue_run)
┌───────────────┴─────────────────────────▼───────────────────┐
│  FastAPI 适配层 (server.py)                                  │
│  SSE 管道 · 多题运行锁 · 工作区浏览接口 · 外观/产物静态服务     │
└───────────────▲─────────────────────────┬───────────────────┘
                │ graph.astream            │ checkpointer
┌───────────────┴─────────────────────────▼───────────────────┐
│  LangGraph 编排内核 (agent.py)                               │
│  StateGraph · Send 扇出 · 超步屏障 · interrupt 人机协同       │
│  ┌──────────┐   ┌─────────────────────────────┐             │
│  │ 建模/质检 │──►│ solve_with_method × N(并行)  │──► 汇总对比  │
│  └──────────┘   └─────────────────────────────┘   ──► 论文   │
└───────────────▲─────────────────────────────────────────────┘
                │ OpenAI 兼容协议 (ChatDeepSeek / 捕获子类 ChatOpenAI)
        ┌───────┴────────┐
        │ DeepSeek/GLM/  │  ← .env 密钥, thinking 参数矩阵
        │ Qwen/Kimi/…    │
        └────────────────┘
```

## 2. 技术栈清单

| 分层 | 选型 | 版本 | 职责 |
|---|---|---|---|
| 编排内核 | LangGraph | 1.x | 状态机编排、Send 并行扇出、interrupt 人机协同、checkpointer 持久化 |
| LLM 接入 | langchain (core/openai/deepseek/community) | 1.x | OpenAI 兼容协议统一接入、工具绑定、结构化输出 |
| 服务层 | FastAPI + uvicorn | 0.1xx | SSE 流式接口、题目/产物 REST、外观配置持久化 |
| 前端框架 | React 18 + TypeScript + Vite | 18 | 会话 UI、状态桶、SSE 消费（fetch 流式读取器） |
| 桌面壳 | Electron | — | 桌面窗口承载；concurrently 一键并行拉起 vite + electron |
| 文档处理 | pypdf / python-docx / pandas | — | 题目/数据集多格式解析，论文 LaTeX 模板独立副本 |
| 检索工具 | TavilySearchResults (langchain-community) | — | 可选联网查资料工具（模型自主决定是否调用） |
| 可观测 | loguru + 自研用量记账 | — | LLM 调用日志、token/缓存命中分桶、AI 使用事件留痕 |

## 3. 核心机制设计

### 3.1 编排内核：基于 Pregel 语义的并行方法对比

- **扇出**：`send_problem_index` 后由条件边 `dispatch_sends` 为每个候选方法发一个 `Send("solve_with_method", …)`，N 个方法在同一超步内并行求解全部小问（分支身份随 Send 输入全程携带，不经图往返）。
- **屏障**：N 分支写 `done_pairs`（`add_or_reset` 可重置追加 reducer），汇合后统一质检。Pregel 调度保证静态边指向的汇合节点**每超步仅执行一次**（按节点名去重的 PULL 任务），天然等价于屏障。
- **打回**：质检不通过 → `Command(goto="modeling")`，同时以字符串哨兵 `RESET` 经 reducer 归零全部累积态（answers/code_files/done_pairs…），`model_iteration` 上限 3 次强制放行防死循环。
- **汇总**：`compare_summarize` 逐问对比择优并标注每问采用方法；论文撰写以"单主线方法 + 素材全量取用"收敛为一条主线。

### 3.2 双流式输出：节点级 + Token 级

`graph.astream(stream_mode=["updates", "messages"])` 双通道并发：

- `updates` 通道 → 节点完成事件（结构化摘要、code_files、run_report）；
- `messages` 通道 → **LLM token 增量**，经 SSE 以 `token` / `reasoning` 两种事件实时转发，前端按消息 id 分组实现打字机效果。

### 3.3 思维链捕获：只加不改的客户端子类

GLM/Qwen/Kimi 等 OpenAI 兼容推理模型把思考过程放在流式 delta 的 `reasoning_content` 字段，langchain-openai 官方不提取。解决方案是一个仅重写两个转换钩子的 `ChatOpenAI` 子类：字段转换与请求构造全部透传父类 `super()`，仅将思考增量附加进 `additional_kwargs`——**对任何厂商的请求/响应行为零改动**。DeepSeek 走官方 `ChatDeepSeek`（原生携带该能力）。

### 3.4 多题并行隔离：thread_id 贯穿全栈

| 层 | 隔离机制 |
|---|---|
| 编排 | thread_id 即题目 id；工具层经 `get_config()` 解析运行内工作区，并发安全 |
| 并发控制 | 每题一把 `asyncio.Lock`，同题串行、异题并行 |
| 存储 | `workspaces/{题目id}/{question,dataset,code,paper,photo}` 物理目录隔离 |
| 前端 | 每题一个 `WsState` 状态桶（日志/状态/提问/步骤/总结），切题只换视图不掐流；epoch 代数丢弃旧运行残余事件 |

### 3.5 人机协同与断点续跑

- `interrupt()` 在建模思路、质检意见、论文元信息等关键节点挂起等待人工输入；前端作答后 `Command(resume=…)` 恢复。
- `continue_run` 三态语义：线程为空 → 新运行；有挂起 interrupt → 跳过提问继续；中途崩溃 → 从最近 checkpoint 原地续跑（中断节点会重跑）。

### 3.6 多模型工厂：注册表 + 参数矩阵 + 实例缓存

`MODEL_REGISTRY` 声明各家 base_url/model/密钥环境变量/**thinking 参数格式**（DeepSeek、智谱、DashScope 三种方言）。工厂按 `(key, role, api_key)` 缓存实例；role 决定思考开关——工具/结构化角色强制关思考（规避 DeepSeek 思考模式拒绝强制 `tool_choice` 的 HTTP 400），文本角色默认开思考。

### 3.7 容错与降级链

| 故障 | 策略 |
|---|---|
| 结构化提取失败 | 三层降级：with_structured_output → 文本调用 + 手动 JSON 解析 → 整题单问兜底 |
| 思考模型拒绝 tool_choice | `_invoke_llm` 按 400 关键字确定性降级为非思考实例 |
| 瞬时故障（429/超时/5xx） | 2s/4s 退避重试；401/403 鉴权错误快速失败不重试 |
| 单问求解失败 | 单问隔离记入 `failed_qs`，不拖崩分支，交由质检裁决 |
| 搜索工具失败 | 异常转错误工具消息回喂模型，流程不中断 |

### 3.8 质量闭环与合规留痕

- **机器质检**：求解完成后结构化裁决"方案与结果是否可信自洽"，不通过携带原因与修正建议打回；
- **人工审核**：`interrupt` 收集对最终总结的意见，回流重写（清空对话上下文，避免惯性）；
- **数值真实性铁律**：prompt 级约束所有关键数值必须出自脚本真实 stdout（运行报告为唯一可信数值源）；
- **合规留痕**：AI 调用事件（时间/模型/提示摘录）独立记账，自动生成《AI 工具使用详情》PDF。

### 3.9 论文生产线

`writter_struct` LaTeX 模板复制为题目独立副本 → 逐章节 LLM 生成（Nature 式证据链 + 去 AI 味写作规范双约束）→ 方法口径与《最终总结》对齐 → 图片/代码同步进 figures 与附录 → 编译验证产出 `document.pdf`。

## 4. 前端工程要点

- **SSE 消费**：fetch + ReadableStream 手写解析器（含中断信号、跨题 epoch 过滤）；
- **流式渲染**：思考过程（虚线气泡，打字中常开内滚、完成自动折叠可回看）与正文打字机（闪烁光标）分离呈现；节点行随首个 token 实时宣告，规避"气泡先于节点名"的时序倒挂；
- **自动滚动**：requestAnimationFrame 合帧 + 近底部跟随判定，不打断用户回看；
- **主题引擎**：`ui_config.json` → CSS 变量（强调色/文字主色/玻璃配方），`color-mix` 驱动玻璃透明度、模糊、压暗的连续可调；
- **产物面板**：按题查询（thread_id 参数），清单轮询 + 事件驱动双通道刷新，行内删除带运行保护（409）。

## 5. 关键设计权衡

| 决策 | 理由 | 代价 |
|---|---|---|
| agent 内核与展示层零耦合（FastAPI 只做适配） | 图逻辑演进不受 UI 牵制 | 服务层需跟随内核接口版本 |
| 方法并行扇出（对比择优）而非链式流水线 | 同 plan_struct 控制变量，对比择优是论文加分结构 | token 成本 ×N；流式过程交错 |
| 通用 ChatOpenAI 子类捕获思考链，而非换用 DeepSeek 客户端 | 不改变非 DeepSeek 模型的任何请求行为 | 放弃了 ChatDeepSeek 的 DeepSeek 专有兜底 |
| 聊天日志存前端状态桶而非服务端 | 切题零延迟、实现轻 | 刷新页面即丢；跨设备查看需服务端事件留存（演进项） |
| 结构化步骤钉死 DeepSeek 非思考实例 | 规避强制思考模型 400 | 强依赖 DEEPSEEK_API_KEY 的有效性 |
| 并行运行共享全局模型实例 | 保持 agent.py 原始形态 | 后启动的 set_model 影响先启动题的后续调用（用"并行期锁模型"纪律规避） |

## 6. 已知边界与演进方向

1. **服务端事件留存**：聊天日志目前仅前端内存态，服务端加每题事件环形缓冲即可支撑刷新恢复与移动端查看；
2. **模型实例运行级隔离**：以 LangGraph config 携带模型 key、调用点改 `get_llm(role)` 解析，消除全局重绑的并行串扰；
3. **问题索引提取的枚举归一化**：`TYPE_METHODS` 白名单精确匹配，可加同义词映射避免静默退化到兜底方法集；
4. **依赖版本对齐**：实际安装版本领先于 requirements.txt 锁定版本，宜周期性核对行为差异。

---

*本文档由代码实现逆向梳理生成，机制描述均可在 `src/agent.py`、`src/models.py`、`src/tool.py`、`backend/server.py`、`frontend/src/` 中对应验证。*
